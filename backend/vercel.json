{
  "version": 2,
  "builds": [
    {
      "src": "src/main.ts",
      "use": "@vercel/node",
      "config": { "includeFiles": ["prisma/**", "generated/**"] }
    }
  ],
  "scripts": {
    "vercel-build": "prisma generate && nest build"
  }
}

